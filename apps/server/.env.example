PORT=8080
RESUME_PATH=./data/resume.pdf

# Optional: enables embedding-based search (falls back to keywords if missing)
OPENAI_API_KEY=

# SMTP for email tool (use any provider). Example: Gmail with App Password.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM="Your Name <your@gmail.com>"
